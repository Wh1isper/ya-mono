# YA Agent Platform Web

Vite + React + TypeScript shell for the first-party platform UI.

## Surfaces

- admin overview
- chat workspace shell
- bridge inventory

## Development

```bash
cd apps/ya-agent-platform-web
pnpm install
pnpm dev
```

Set `VITE_PLATFORM_BASE_URL` when the backend runs on a different origin.
